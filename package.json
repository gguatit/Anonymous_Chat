{
  "name": "anonymous-chat",
  "version": "1.0.0",
  "description": "Anonymous real-time chat application using Cloudflare Workers and Pages",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-11-01",
    "build": "echo 'No build step required - deploying public directory'",
    "deploy": "wrangler pages deploy public",
    "lint": "eslint src/**/*.js public/**/*.js",
    "test": "echo 'No tests yet - run: npm run lint' && npm run lint"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "websocket",
    "chat",
    "anonymous"
  ],
  "author": "",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "eslint": "^8.54.0",
    "wrangler": "^4.48.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0"
  }
}
